{
  "builds": [
    {
      "src": "./",
      "use": "python3"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/1"
    }
  ],
  "env": {
    "FLASK_ENV": "production",
    "TOKEN": "${YOUR_TELEGRAM_BOT_TOKEN}",
    "URL": "${YOUR_VERCEL_URL}"
  }
}
